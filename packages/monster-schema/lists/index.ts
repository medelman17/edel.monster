import { List } from "./types";

export const bullet: List = { title: "Bullet", value: "bullet" };
export const number: List = { title: "Number", value: "number" };

export default { bullet, number };
